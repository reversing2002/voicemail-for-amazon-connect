name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  safety-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Security placeholder
        run: echo "Info-stealer workflow removed. Add project-specific CI here."
